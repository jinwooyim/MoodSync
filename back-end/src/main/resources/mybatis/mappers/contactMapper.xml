
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.contact.dao.ContactDAO">
	<insert id="insertContact">
		insert into user_contact(CONTACTID, USERNUMBER, CONTACT_TITLE, CONTACT_CONTENT) values((SELECT NVL(MAX(CONTACTID), 0) + 1 FROM
		user_contact), #{userNumber}, #{contact_title}, #{contact_content});
	</insert>
</mapper>

